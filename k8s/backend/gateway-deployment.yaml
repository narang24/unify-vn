apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: unify

spec:
  replicas: 1

  selector:
    matchLabels:
      app: gateway

  template:
    metadata:
      labels:
        app: gateway

    spec:
      containers:
        - name: gateway

          image: <YOUR_ECR_URI>:latest

          imagePullPolicy: Always

          ports:
            - containerPort: 8000

          env:
            - name: SERVICE
              value: gateway

          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret